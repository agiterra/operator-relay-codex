---
description: Start relaying operator prompts from an ephemeral agent to you.
allowed-tools: mcp__plugin_operator-relay_operator-relay__operator_relay_start
argument-hint: "<agent_id> [notify_id]"
---

# Start Operator Relay

Parse `$ARGUMENTS` for agent ID and optional notify ID (space-separated).
Call `operator_relay_start` with agent and notify.
Report the result.
